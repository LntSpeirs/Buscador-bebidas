import { Modal } from "react-bootstrap";

const ModalBebida = () => {
  const handleModalClick = () => {};
  return <Modal show={true} onHide={handleModalClick}></Modal>;
};

export default ModalBebida;
